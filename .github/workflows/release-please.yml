name: Release Please

on:
  push:
    branches: [main, sgc] # updates/opens the release PR when commits land on main or sgc
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write
  id-token: write

concurrency:
  group: release-please
  cancel-in-progress: true

jobs:
  release-please:
    name: Release Please
    runs-on: ubuntu-latest
    outputs:
      release_created: ${{ steps.rp.outputs.release_created }}
    steps:
      - id: rp
        uses: googleapis/release-please-action@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          config-file: release-please-config.json

      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Generate i18n catalog
        if: ${{ steps.rp.outputs.prs_created == 'true' || steps.rp.outputs.prs_updated == 'true' }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RP_PR_OUTPUT_JSON: ${{ steps.rp.outputs.prs }}
        run: |
          PR_NUMBER=$(echo "$RP_PR_OUTPUT_JSON" | jq -r '.[0].number')
          gh pr checkout "$PR_NUMBER"

      - name: Setup Mise
        if: ${{ steps.rp.outputs.prs_created == 'true' || steps.rp.outputs.prs_updated == 'true' }}
        uses: immich-app/devtools/actions/use-mise@cd24790a7f5f6439ac32cc94f5523cb2de8bfa8c # use-mise-action-v1.1.0
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and update i18n catalog
        if: ${{ steps.rp.outputs.prs_created == 'true' || steps.rp.outputs.prs_updated == 'true' }}
        run: |
          mise install:frozen
          mise build:deps
          mise lingui

          if git diff --quiet; then
            echo "No changes to i18n catalog"
          else
            git config user.name "Stoat CI"
            git config user.email "stoat-ci@users.noreply.github.com"
            git add -A
            git commit --signoff -m "chore: generate i18n catalog"
            git push
          fi

  publish-stable:
    name: Publish release to stable branch
    needs: release-please
    if: needs.release-please.outputs.release_created == 'true'
    runs-on: ubuntu-latest

    permissions:
      contents: write
    
    steps:
      - uses: actions/checkout@v4
      - run: |
          git fetch origin sgc:sgc || true
          git push origin ${{ github.ref_name }}:sgc -f
